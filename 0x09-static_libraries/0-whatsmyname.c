#include <stdio.h>

/**
 * main - Prints name
 * @argc: Argument count
 * @argv: Argument vector
 *
 * Return: Always 0 (Success)
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%\n", argv [0]);

	return (0);
}
