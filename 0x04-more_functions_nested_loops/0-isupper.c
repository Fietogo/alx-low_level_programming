#include "main.h"
/**
 * _isupper - checks if uppercase
 * @c: character to check
 * 
 * Returns 1 if c is uppercase
 * Returns 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
