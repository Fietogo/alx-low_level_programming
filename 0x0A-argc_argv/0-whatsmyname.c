#include <stdio.h>

/**
 * main - Prints name
 * @argc: Argument count
 * @argv: Argument vector
 *
 * Return: Always 0 (Succcess)
 */

int main(int_attribute_((_unused)_) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
