#include <stdio.h>

/**
 * main - Prints name
 * @argc: Argument count
 * @argv: Argument vector
 *
 * Return: Always 0 (Success)
 */

int main(int _attribute_((_unused_)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}

